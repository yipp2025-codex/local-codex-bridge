# Codex isolated workspace

This directory is the only workspace exposed to run_codex_prompt.
It intentionally contains no credentials, runtime logs, tunnel state, or executable files.
The bridge does not write project files through this capability.
